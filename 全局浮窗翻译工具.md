**全局浮窗翻译工具** AI编写
 
初始需求 – 划词翻译，鼠标上方悬浮，任何应用可用
↳ 创建基础程序：pynput 监听鼠标，模拟 Ctrl+C 获取文本，deep-translator 翻译，tkinter 显示半透明悬浮窗。

弹窗位置随机、占用剪贴板 – 修复 DPI 缩放、剪贴板保护
↳ 开启 Windows DPI 感知，用 after 替代线程销毁弹窗；备份并立即还原剪贴板，缩短占用时间。

缺少托盘退出 – 无关闭方式
↳ 添加 pystray 系统托盘图标，右键菜单“退出”，生成自定义图标。

截图快捷键失效 – 全局键盘状态残留
↳ Windows 改用底层 keybd_event 模拟 Ctrl+C，强制释放左右 Ctrl，不再使用 pynput 键盘控制器。

重复弹窗、字体过小 – 重复翻译、字号不适
↳ 添加 last_text 去重；字体改为 12 号，并引入配置区。

截图后无法粘贴 – 剪贴板还原覆盖图片
↳ 完全移除剪贴板备份/还原，仅读取新内容，若与旧内容相同则忽略。

翻译速度慢 – 在线请求延迟
↳ 添加翻译缓存（最近 500 条），相同原文免网络请求。

弹窗内容无法选中复制 – 仅显示不可交互
↳ 将 Label 改为只读 Text 组件，允许划选复制。

缺少离线词库 – 在线依赖高延迟
↳ 下载并精简 ECDICT 全量词典（77万+），纯英文单词本地毫秒级翻译。

词典文件过大无法上传 – GitHub 文件大小限制
↳ 在 GitHub Actions 中在线下载原始词典，裁剪仅保留 word+translation 字段，生成约 15MB 精简 CSV 并打包进 EXE。

构建失败（编码错误） – 日志中文输出导致 cp1252 错误
↳ 在 Actions 步骤中增加 export PYTHONIOENCODING=utf-8。

EXE 运行无反应 – 打包后词典路径错误、控制台隐藏无法排错
↳ 修正 _MEIPASS 路径读取；暂时去除 --windowed 显示控制台；增加 translator.log 日志文件；托盘气泡提示启动成功。

在线翻译错误 – requests.models.DEFAULT_TIMEOUT 属性不存在
↳ 移除该超时设置，由 deep-translator 自行处理。

选中文本日志延迟 – 松开鼠标后日志未即时显示
↳ 增加重试机制，模拟 Ctrl+C 后等待 0.12 秒并最多尝试两次；在 on_click 中额外延迟 0.1 秒。

频繁出现 “undefined” 翻译 – 网页残留垃圾文本
↳ 新增 is_valid_text() 过滤黑名单（undefined 等）和纯符号/数字；剪贴板变化检测确保仅当内容变化且有效才触发翻译。

闪退、鼠标悬停浮窗消失 – 未捕获异常、无停留阅读
↳ 全局异常钩子 sys.excepthook + 关键函数 try-except；浮窗绑定 <Enter>/<Leave>，鼠标移入暂停计时，移出后 1.5 秒消失。

日志格式与性能卡顿 – 频繁写日志导致卡顿
↳ 日志分级（LOG_LEVEL），默认仅记录错误/关键事件；等待时间微调；弹窗尺寸优化。

字号调整为 18 – 最终视觉调优
↳ FONT_SIZE 改为 18，满足用户偏好。

最终打包优化 – 取消控制台窗口，日志改为 .txt
↳ 恢复 --windowed 打包为无控制台模式；LOG_FILE 改为 translator.txt 便于记事本直接打开。
